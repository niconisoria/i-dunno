---
title: Health Check Endpoint
refs: []
---

## Brainstorm

GET /health returns 200. Used by load balancer.

Related: none

## Story

As ops, want health endpoint, so load balancer can verify app alive.

AC:
1. GET /health returns 200
2. Response body includes uptime in seconds
3. No auth required

## Design

### Flow

```
┌──────────────┐     ┌────────────┐     ┌──────────────────┐
│ GET /health  │ ──▶ │ build resp │ ──▶ │ 200 + { ok, up } │
└──────────────┘     └────────────┘     └──────────────────┘
```

### Data

```
input:  none
output: { status: "ok", uptime: integer }
```

### Modules

- `app/routes.rb` — add GET /health route
- `test/health_test.rb` — create

## Summary

Built GET /health returning 200 with status and uptime. No auth. Route added to app/routes.rb. Uptime from process start time.

[routes.rb](../../app/routes.rb) [health_test.rb](../../test/health_test.rb)
